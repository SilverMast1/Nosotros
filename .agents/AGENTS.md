# Reglas del Proyecto (proyectoNosotros)

- **Subir cambios automáticamente**: Al finalizar las tareas o cuando se completen cambios en el código, siempre realiza un commit y súbelo (`git push origin main`) al repositorio remoto de GitHub de forma automática antes de terminar el turno.
